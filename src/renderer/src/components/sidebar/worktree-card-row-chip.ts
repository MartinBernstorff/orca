/** Shared geometry for the interactive chips on a worktree card's linked-refs row,
 *  so the ref links and the delete quick action stay the same size and shape. */
export const WORKTREE_CARD_ROW_CHIP_CLASS =
  'inline-flex h-6 items-center rounded px-2 text-[10px] leading-none transition-colors'
